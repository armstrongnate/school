include <iostream> if you are going to interact with user.

returning 0 means the program worked.

a block of code is contained in curly braces {}

anything that isn't a block of code ends in a semicolon. 

std::cout => I want to send something to the terminal (ie print)

<< => output operator. Whatever comes next needs to go out to the screen.

strings must be wrapped in double quotes.

std::endl => make a line break.

int => define a function (ie def)

must declare the type of your arguments. Example: int add(int a, int b). Declaring a and b as integers.

must declare a variable's type before you can give it an assignment.
